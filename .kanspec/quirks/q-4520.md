---
id: q-4520
title: git log --name-only lists paths relative to the repo root; they only join with discovery paths when the scanned root IS the repo root. Scanning a subdirectory silently yields empty history.
paths: [src/history/**]
severity: gotcha
status: fixed
source: t-4adb
fixed_by: t-8e91
---
git log --name-only lists paths relative to the repo root; they only join with discovery paths when the scanned root IS the repo root. Scanning a subdirectory silently yields empty history.
