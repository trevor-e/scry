---
id: q-f7da
title: Python import roots that are only the importer's ancestors never reach src/<pkg>/ from tests/; on a src layout every test reference silently disappears and every file gets the no-tests boost
paths: [src/deps/**]
severity: gotcha
status: active
source: t-c0ce
fixed_by: null
---
Python import roots that are only the importer's ancestors never reach src/<pkg>/ from tests/; on a src layout every test reference silently disappears and every file gets the no-tests boost
