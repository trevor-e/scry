---
id: q-542e
title: Periodic code (repeated near-identical lines) matches itself on every shifted diagonal; without the overlap suppression in detect() one 20-line block yields dozens of pairs
paths: [src/clones/**]
severity: gotcha
status: active
source: t-9c0b
fixed_by: null
---
Periodic code (repeated near-identical lines) matches itself on every shifted diagonal; without the overlap suppression in detect() one 20-line block yields dozens of pairs
