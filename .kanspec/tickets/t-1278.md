---
id: t-1278
title: 'Merge main into the analyzer branch: 10 conflicting files, parse-once vs the per-pass tree walks, two mass-edit fix gates, tsconfig resolution vs edge kinds'
state: doing
spec: null
proposal: null
item: null
deps: []
followup_of: null
discovered_in: null
branch: ks/t-1278-merge-main-into-the-analyzer-branch-10-c
worktree: null
claimed_by: claude/021cf45b-81f4
pr: null
head: null
spec_unchanged: null
created: 2026-09-16T02:47:38Z
---
Merge main into the analyzer branch: 10 conflicting files, parse-once vs the per-pass tree walks, two mass-edit fix gates, tsconfig resolution vs edge kinds

## Steps

## Log
- 2026-09-16T02:47Z  todo     claude/021cf45b-81f4  new
- 2026-09-16T02:47Z  doing    claude/021cf45b-81f4  start (branch created)

## Conflicts

Recorded before resolution so a later problem can be traced to the merge. `origin/main` gained 10 commits after this branch was cut (2423370 parse once, 946188e whole-word logic markers, 6297588 tsconfig / src-layout resolution, 5efc9de mass edits are not fixes + "no test file imports it", plus kanspec housekeeping). A dry-run merge (`git merge-tree --write-tree`) conflicts in:

- `src/main.rs`: main's `parse_once` (one tree per file shared by metrics, deps, clones, with `Language::parse` and a thread-local parser cache) versus this branch's `metrics::analyze_all_with` closure that hands the metrics tree, test regions and units to mentions, dead and comments, plus eight new subcommands and the scan wiring for ten new passes.
- `src/metrics/mod.rs`: main's `analyze_tree` / `collect` split versus this branch's `analyze_all_with`, `unit_nodes`, `unit_name_of`, `cognitive_rebased`, locals, `in_test`.
- `src/clones/mod.rs`: main's `tokenize(tree)` / `detect_from` versus this branch's tokenizer with byte offsets, test-region sentinels, table tagging and same-container self-match drop.
- `src/deps/mod.rs`: main's `imports` / `build_from` / `walks_imports`, tsconfig and src-layout resolution, deterministic cycle order versus this branch's edge kinds, symbol counts and cycle cuts.
- `src/history/mod.rs`: main's `is_fix && !mass_edit` versus this branch's `[history] fix_mass_edit_cap` knob and `bot_patterns`: the same rule implemented twice.
- `src/report/mod.rs`: main's "no test file imports it" wording versus this branch's graded `test_units` reason and the new sections.
- `src/config.rs`: main's tsconfig-related knobs versus this branch's new sections.
- `.kanspec/specs/deps.md`, `history.md`, `report.md`: both sides added Rules.

Resolution intent: keep main's parse-once shape and route this branch's tree consumers onto that single tree; keep one mass-edit fix gate; keep this branch's reason wording where it replaced main's.

## Resolution (merge commit 50d7bd2)
- Kept main's parse-once shape: `parse_once` parses each file once and feeds metrics + the seven walkers (via `metrics::analyze_tree_with`), `deps::imports` and `clones::Tokenizer::tokenize` (owned tree kept for the container walks); Test files' trees are kept for the test-indexing passes.
- One fix gate: `fix_mass_edit_cap` (default on) implements main's `is_fix && !mass_edit`; main's test kept.
- Kept `TsConfigs` / `py_roots` alongside the branch's edge kinds, symbol counts and cuts; kept main's deterministic cycle sort; kept the branch's `test_units` reason wording with main's "not coverage" caveat; merged all three spec files' Rules.
- Verified: 173 tests pass, clippy warning multiset unchanged, every corpus scan is unchanged vs the pre-merge branch except what main's own commits explain (kanspec `src/cli.rs` reclassified as data by main's whole-word density rule; click `__init__.py` gains 12 importers from src-layout roots; hono's 5 cycles reordered by the deterministic sort).
